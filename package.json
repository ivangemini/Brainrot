{
  "name": "pigeon-maxxing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "art:generate": "node tools/art/generate-raster-assets.mjs",
    "predev": "npm run art:generate",
    "dev": "vite",
    "prebuild": "npm run art:generate",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
