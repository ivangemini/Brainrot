import chunk00 from './chunk-00';
import chunk01 from './chunk-01';

/**
 * Compact WebP derived from the approved meme-pigeon reference supplied by the user.
 * Kept as raster data in source because the GitHub connector is text-only.
 * This is still a raster production asset; do not replace it with SVG/vector art.
 */
export const MEME_PIGEON_HERO_DATA_URL = `data:image/webp;base64,${chunk00}${chunk01}`;
